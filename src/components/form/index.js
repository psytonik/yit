import React from 'react';import './form.css';const Form = ({setCategory,setTitle,setDescription,setAuthor,setPicture,setColors}) => {    const options = ['Red','Blue','Green','Purple'];    const handleChange = event => {        setColors(event.target.value)    }    return (        <form className="form">            <div>                תמונות                <div className="pictureGallery">                    <input type="text"/>                    <button type="click">מחק</button>                </div>            </div>            <div className="addMore">                <input type="file"/>                <span>                    הוסף תמונה                </span>            </div>            <div className="coloredCategoryName">                <label htmlFor="color">צבע רקע</label>                <select name="color" id="color" onChange={handleChange}>                    <option>בבקשה תבחר צבע</option>                    {options.length >0 && options.map((c,i)=>(                        <option key={i} value={c}>                            {c}                        </option>                    ))}                </select>                <label htmlFor="category">שם קטגוריה</label>                <input                    type="text"                    id="category"                    maxLength="45"                    onChange={e=> setCategory(e.target.value)}                />            </div>            <label htmlFor="title">כותרת</label>            <textarea                name="title"                id="title"                rows={3}                maxLength="55"                onChange={e=> setTitle(e.target.value)}            />            <label htmlFor="description">כותרת משנה</label>            <textarea                name=""                id="description"                rows={3}                maxLength="160"                onChange={e=>setDescription(e.target.value)}            />            <div>                <label htmlFor="authorName">שם מחבר</label>                <input                     type="text"                     id="authorName"                    maxLength="45"                    onChange={e=> setAuthor(e.target.value)}                />            </div>        </form>    );};export default Form;