import React, {useState} from 'react';import './card.css';const Card = ({picture,colors,category,title,description,author}) => {    const [currentImage,setCurrentImage] = useState(0)    const goBack = () => {        if(currentImage > 0){            setCurrentImage((currentImage)=>currentImage-1)        }    }    const goNext = () => {        if(currentImage < picture.length -1) {            setCurrentImage((currentImage) => currentImage + 1)        }    }    return (        <div className="card">            <div className="picture">                    <div className="pictureInner">                        <img                            src={picture[currentImage]}                            alt="hotel in tel aviv"                            className="image"                            />                    </div>                {picture.length > 1 && <>                    <span className="prev" onClick={goBack} >&#10095;</span>                    <span className="next" onClick={goNext} >&#10094;</span>                </>}            </div>            <div className="category">                <span className="color" style={{background:`${colors}`}}>                    {category}                </span>            </div>            <div className="title">                <h1>                    {title}                </h1>            </div>            <p className="description">                {description}            </p>            <div className="authorName">                {author}            </div>        </div>    );};export default Card;